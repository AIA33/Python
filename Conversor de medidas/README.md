Es necesario instalar requirements.txt para hacer uso del script, utilizando: 

```pip install -r requirements.txt```

y luego utilizar:

```python conversor_medidas.py```

el cual generará el archivo final 'muebles_medidas_convertidas.xlsx' 
